---
to: src/app/components/common/<%= h.inflection.camelize(name.replace(/\s/g, '_')) %>/index.jsx
---
import {h} from 'hyperapp'
import cc from 'classcat'

import './style.css'

export const <%= h.inflection.camelize(name.replace(/\s/g, '_')) %> = ({label = label || 'Label', name = name || 'name', type = type || 'text', placeholder = placeholder || ' ', required, hint, ...rest}) => (
  <div class={cc(['<%= h.inflection.dasherize(name.toLowerCase()) %>', name])} key={name}>
    {hint ? <p class="hint">{hint}</p> : null}
    <input type={type} name={name} id={name} placeholder={placeholder} required={required} {...rest} />
    <label for={name}>{label}{required ? ' *' : null}</label>
  </div>
)

/*
import {<%= h.inflection.camelize(name.replace(/\s/g, '_')) %>} from './components/common/<%= h.inflection.camelize(name.replace(/\s/g, '_')) %>'
<<%= h.inflection.camelize(name.replace(/\s/g, '_')) %> label="First name" name="firstName" value={firstName} hint="Optional hint" />
*/
