---
to: src/app/components/<%= h.inflection.camelize(name.replace(/\s/g, '_')) %>.jsx
---
import {h} from 'hyperapp'


export const <%= h.inflection.camelize(name.replace(/\s/g, '_')) %> = () => (
  <div key="<%= h.inflection.dasherize(name.toLowerCase()) %>">
    New <%= name %> component!
  </div>
)

/*
import {<%= h.inflection.camelize(name.replace(/\s/g, '_')) %>} from './components/<%= h.inflection.camelize(name.replace(/\s/g, '_')) %>'
<<%= h.inflection.camelize(name.replace(/\s/g, '_')) %> />
*/