---
to: src/components/<%= h.inflection.camelize(name.replace(/\s/g, '_')) %>/actions.js
---

export const <%= h.inflection.camelize(name.replace(/\s/g, '_'), true) %>Actions = {

  // Nested shallow merge action
  set<%= h.inflection.camelize(name.replace(/\s/g, '_')) %>: fragment => state => ({
    <%= h.inflection.camelize(name.replace(/\s/g, '_'), true) %>: {
      ...state.<%= h.inflection.camelize(name.replace(/\s/g, '_'), true) %>,
      ...fragment
    }
  })
}