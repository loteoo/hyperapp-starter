---
to: src/app/components/<%= h.inflection.camelize(name.replace(/\s/g, '_')) %>/actions.js
---

export const nestedSetter = (state, nestedState) => ({
  ...state,
  <%= h.inflection.camelize(name.replace(/\s/g, '_'), true) %>: nestedState
})

export const SetValue = (state, key, ev) => nestedSetter(state, {
  ...state.<%= h.inflection.camelize(name.replace(/\s/g, '_'), true) %>,
  [key]: ev.target.value
})

export const SubmitForm = (state, ev) => {
  ev.preventDefault();
  alert('Submitted!\nForm state: \n\n' + JSON.stringify(state.<%= h.inflection.camelize(name.replace(/\s/g, '_'), true) %>, null, 2));
  return nestedSetter(state, {
    ...state.<%= h.inflection.camelize(name.replace(/\s/g, '_'), true) %>,
    submitted: true
  })
}
